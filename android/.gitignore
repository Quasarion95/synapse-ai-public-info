# Сборка пересоздаётся из исходников, в репозитории ей делать нечего.
build/
app/build/
.gradle/
local.properties

# Ключ подписи не попадает в репозиторий никогда: он публичный, а ключ — это
# право выпускать обновления от вашего имени. Хранится только на маке.
*.keystore
*.jks
keystore.properties
